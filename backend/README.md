# Backend Folder

This folder is reserved for the FastAPI backend code.

## Setup Instructions for Partner 2:
1. Initialize your Python virtual environment here.
2. Create your `main.py` and `requirements.txt`.
3. The frontend expects endpoints at `/upload-image` and `/reports`. (Currently, the frontend points to `http://localhost:8000`).
